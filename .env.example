# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY= # used only in server actions/cron, not exposed client-side

# Email (Brevo / any SMTP)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="OneSearch Job <noreply@onesearch.dev>"

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Security
ENCRYPTION_KEY= # 32 chars for token encryption at rest (if needed)
ADMIN_TOKEN= # A secret token to protect manual trigger endpoints
